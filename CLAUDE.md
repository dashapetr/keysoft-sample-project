# Universal engineering standards. Kept short; API specifics live in an @path module.
- Use the logging logger for diagnostics; never print().
- Type-hint every public function.

@.claude/standards/api.md          # @path import — pulls in the API contract below